// 7 - Escreva uma função que receba dois valores inteiros lidos no programa principal e efetue as seguintes operações matemáticas: adição, subtração, multiplicação, divisão e módulo (resto da divisão). 
// Para os valores de entrada 5 e 2, a função deverá exibir na tela:
// Observe que: 
// Para imprimir o símbolo %, você precisa usar %%, como segue o exemplo:
// printf("%d %% %d = %d\n", ...

/*
#include <stdio.h>

void num7(int num1, int num2) 
{
    int soma = num1 + num2;
    int diferenca = num1 - num2;
    int produto = num1 * num2;
    int quociente = num1 / num2;
    int resto = num1 % num2;

    printf("Soma:%d + %d = %d\n", num1, num2, soma);
    printf("Diferenca: %d - %d = %d\n", num1, num2, diferenca);
    printf("Produto: %d * %d = %d\n", num1, num2, produto);
    printf("Quociente: %d / %d = %d\n", num1, num2, quociente);
    printf("Resto: %d %% %d = %d\n", num1, num2, resto);
}

int main() 
{
    int num1, num2;

    printf("Digite um valor: ");
    scanf("%d", &num1);

    printf("Digite um segundo valor: ");
    scanf("%d", &num2);

    num7(num1, num2);

    return 0;
}
*/
